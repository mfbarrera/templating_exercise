

function saludar (lista) {
    lista.forEach((nombre, index) => {
        console.log ("Hola " + nombre + " Alumno numero " + index)
        console.log ("Hola " + nombre + " Alumno numero " + index)
    });
}

let alumnos = [" Fernando", "Juan", "Arley"]

saludar (alumnos);
